# Global Memory

Shared knowledge is stored at `/workspace/global/memory/`. Check `INDEX.md` there for a list of available files, then `Read` what you need for context beyond your group's own memory.
